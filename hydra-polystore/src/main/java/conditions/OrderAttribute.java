package conditions;

public enum OrderAttribute implements Attributes{
	orderID, freight, orderDate, requiredDate, shipAddress, shipCity, shipCountry, shipName, shipPostalCode, shipRegion, shippedDate
}
